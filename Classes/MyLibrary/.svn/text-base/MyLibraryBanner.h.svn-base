#import <UIKit/UIKit.h>

#define BANNER_W (234.0f)
#define BANNER_H (60.0f)

@interface MyLibraryBanner : UIView <UIWebViewDelegate>{
	UIView* baseView;
	UIWebView* bannerView;
	
}

@property (nonatomic, retain) UIView* baseView;
@property (nonatomic, retain) UIWebView* bannerView;

@end
